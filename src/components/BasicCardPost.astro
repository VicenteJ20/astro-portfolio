---
const { title, description, slug, date, id } = Astro.props
---

<div class='bg-zinc-950 p-4 rounded-lg bg-opacity-45 border border-zinc-700'>
  <h3 class='text-lg font-medium text-zinc-100  transition-all duration-300 ease-in-out'>
    <a class='hover:underline' href={`/blog/${slug}`}>{title}</a>
  </h3>
  <p class='text-zinc-300 truncate'>{description}</p>
</div>